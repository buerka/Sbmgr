# Web 管理界面验收

final result: passed

## 目标与证据

- 目标：把 sbmgr 管理面板迁移到参考 Cloudreve 的 React / TypeScript / MUI 技术栈与浅色工作台风格，保留现有管理操作和 Go 单二进制部署。
- 视觉参考：[Cloudreve 官方工作台截图](https://raw.githubusercontent.com/cloudreve/docs/master/images/homepage.png)、[官方演示登录页](https://demo.cloudreve.org/session)。技术参考为官方 frontend 仓库的 `package.json`、`vite.config.ts` 和 `src/App.tsx`。
- 实现：本地完整 Go 二进制的 `/#/ops`、`/#/users`、用户详情、MUI 菜单/弹窗与登录页。使用隔离测试数据，未使用生产代理凭据。
- 已在浏览器同一次对照中展示官方参考和最终实现。参考图片原始尺寸 3334 × 2188，浏览器参考截图 1280 × 720；实现按 1366 × 900 CSS 像素与 390 × 844 CSS 像素检查，最终设备缩放为 1。参考含外部窗口边框、英文文件管理器内容；此次为风格适配，不把这些产品差异当作像素一致性问题。

## 修复与复核

- P2：原深色侧栏、宣传式登录区与目标不符。已替换为浅灰导航、白色主工作区、蓝色选中态和居中 MUI 登录表单，重新截图对照通过。
- P2：用户和设备操作平铺造成拥挤。已改用 MUI Menu 分组，保留所有原操作；键盘 Escape、焦点恢复、菜单到表单的跳转已通过组件测试与浏览器检查。
- P2：小字偏淡、窄屏操作密集。已统一使用 MUI 文字颜色层级；390px 下导航变抽屉、表单单列，页面实测 scrollWidth 不大于视口，宽表格仅在容器中滚动。
- P2：MUI DialogTitle 的自动 ID 与自定义标题 ID 重复，导致辅助技术读出多余内容。已明确区分容器与标题 ID，并新增精确对话框名称断言。
- 对照后无待修复 P0/P1/P2。字体、工具栏、列表行、菜单和弹窗均做过局部检查；无需依靠仅含整页缩略图的判断。

## 五项视觉检查

| 表面 | 结论 |
| --- | --- |
| 字体与层级 | 本地 Roboto 字体与系统中文后备字体；正文、说明、标题分级一致，字段标签可读。 |
| 间距与布局 | 224px 导航、紧凑路径栏、12px 主圆角；桌面列表与窄屏抽屉无页面溢出。 |
| 颜色 | 浅灰背景、白色内容、MUI 蓝色主按钮与选中项；警告、错误、成功独立呈现。 |
| 图像与图标 | MUI 官方矢量图标与本地字体，无外部 CDN、截图充当界面或字形图标。 |
| 文案与内容 | sbmgr 管理内容保持独立；表单继续说明作用域、默认值和生效要求。 |

## 功能与构建验证

- 浏览器完成登录、用户搜索、菜单/弹窗、测试用户配额保存、状态更新和手机抽屉操作；控制台未发现错误或警告。
- Vitest / React Testing Library：6 项通过，覆盖菜单键盘行为、表单字段边界、真实组件提交与刷新时保持搜索。
- TypeScript 检查、Vite 生产构建、Go vet、Go 全量测试、Linux 构建与全量 Linux 测试通过；Linux HTTP 降权集成及部署脚本自检通过。
- CSP 使用独立响应 nonce 支持 Emotion，未放宽为 unsafe-inline；静态资源路径、nonce 与缺失构建的失败行为有 Go 回归测试。
- npm 依赖审计无已知漏洞。发布流程先按锁文件构建前端，再由 Go embed 打包；构建资源和 node_modules 不入库。

完成项：组件迁移、页面适配、交互回归、桌面/窄屏对照、单文件打包与构建验证。
