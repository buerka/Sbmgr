# Shadcn Admin 设计验收

## 结果与范围

用户选定 https://shadcn-admin.netlify.app/users 作为视觉参考。采用 Inter、slate 明暗变量、Radix / shadcn 组件、Lucide 图标、侧栏、紧凑表格与居中编辑表单，接入现有管理 API。最终本地内嵌版本已完成浏览器验收；没有未解决的 P0 / P1 / P2 问题。本报告记录设计验收时的结果，该阶段未改动生产服务器、用户身份、授权与订阅配置；后续部署单独验收。

此前浏览器自动审批服务连接中断，验收曾暂停。用户明确回复“继续”后恢复同一本地演示环境的检查，未绕过审批限制。

## 对照证据

所有截图均来自真实浏览器，保存于忽略目录 `.dist/shadcn-qa/`，不进入发布源码。

| 检查 | 参考图 | 实现图 | CSS 视口与截图像素 |
| --- | --- | --- | --- |
| 桌面全页构图 | `reference-users-final.png` | `final-users-dark.png` | 均为 1280 × 720 CSS；参考 1270 × 714 像素，实现 1280 × 720 像素 |
| 编辑器局部细节 | `reference-editor-final.png` | `final-editor-dark.png` | 同上；均为深色、打开已有用户的编辑窗口 |
| 窄屏编辑器 | `reference-mobile-editor.png` | `final-mobile-editor.png` | 均为 390 × 844 CSS；参考 380 × 822 像素，实现 390 × 844 像素 |
| 浅色主题 | 同一参考的 slate 变量 | `final-users-light.png`、`final-mobile-light.png` | 实现分别为 1280 × 720、390 × 844 像素 |
| 抽屉修复 | `before-mobile-navigation.png`（修复前） | `final-mobile-navigation.png` | 修复后 390 × 844 CSS / 像素；位置 x=0、y=0，宽 288、高 844 |

参考捕获时 browser devicePixelRatio 为 1.5；最终列表与窄屏实现捕获时约为 1。宿主将参考截图缩放到上述像素尺寸，比较以相同 CSS 视口和区域比例对齐，按约 0.992 / 0.974 的参考捕获比例识别密度差异；没有把宿主缩放列为设计缺陷，也不声称逐像素复刻。

完整列表的参考和最终实现同时送入一次比较输入；桌面编辑器、窄屏编辑器各自另以成对图片送入同一比较输入，直接检查可读的字段、边框、控件、留白和页脚。编辑器图中的表单区域足够清晰，未另外裁切。浅色、抽屉、保存失败截图补充状态验收。

实现展示隔离环境的 3 位代理用户，参考展示示例员工列表，因此行数、业务列、导航项目不同。保留 sbmgr 品牌、实际服务器操作、中文说明与原值提示；没有复制员工邮箱、第三方登录或虚构统计。桌面编辑器宽 560px（参考约 512px），增加分组与帮助文字；手机改用上下排列的字段和固定操作区。这些是现有管理功能的适配，不属于未修复的设计漂移。

其他实际渲染证据：`final-overview.png`、`final-routes.png`、`final-subscriptions.png`、`final-operations.png`、`final-user-policies.png`。均在 1280 × 720 CSS 视口捕获，宿主截图为 1265 × 712 像素。窄屏列表、运维与保存错误见 `final-mobile-users.png`、`final-mobile-operations.png`、`final-save-error.png`。

## 发现与迭代

1. **[P2，已修复] 表格密度偏松。** 初版 36px 行操作按钮使行高大于参考。改为 32px 按钮后，最终浏览器实测每行约 49px；`final-users-dark.png` 与参考重新比较通过。
2. **[P1，已修复] 窄屏导航部分移出屏幕。** 首次补充 `translate: none` 后，构建优化仍保留了通用弹窗的独立位移，实际抽屉坐标为负。将 Dialog 的居中和左侧定位分为显式 placement，左侧不再包含居中位移类。最终实测抽屉完整位于视口内，导航可切换页面并自动关闭；证据见前后抽屉截图。没有靠覆盖重要样式或改变 CSP 修复。
3. **[P2，已修复] 手机搜索图标缺少可访问名称。** 增加明确的 aria-label；浏览器和新增交互回归均通过，可从该入口搜索页面与用户。
4. React 18 的 Radix Portal ref 兼容已修复。动态滚动锁样式使用服务端已有 nonce，CSP 未放宽；最终内嵌页面打开菜单、选择器、编辑器、抽屉后，浏览器 warn / error 为 0。
5. 客户端入口接口采用完整对象替换。编辑模型保留未修改的 short-id 等可选字段，回归验证只改端口不会清空其他现值。其余支持补丁的编辑操作只提交变化项。

## 五项设计表面

- **字体与层级：** 本地内嵌 Inter 400/500/600/700；中文采用系统无衬线回退。标题 24px、正文与表单 14px、表格 13px、辅助说明 12px。与参考同屏检查字重、行高、换行和字段可读性，无待修复问题。
- **间距与布局：** 256px 桌面侧栏、80px 顶部区域、24px 分段节奏、8–10px 圆角。表格紧凑，操作统一靠右；长表单内部滚动。390px 手机宽度下编辑器位于 x=16、y=12，宽 358、高 820；底部操作区位于视口内。
- **颜色与状态：** 使用上游 oklch slate 明暗变量；中性色主按钮、绿色启用标签、明确的错误状态。实际检查浅深色表格、表单、焦点和禁用状态；没有残留上一版 MUI 配色。
- **图标与资产：** Lucide 图标保持一致的尺寸和线条；sbmgr 使用自身 Network 品牌标识。无需要替代的照片或插画，无用手工图形冒充参考资产。上游、Lucide 和 Inter 许可保留在第三方声明中。
- **内容与功能：** 每项编辑先显示当前值；保留单位、范围、清空语义和生效提示。空列表明确说明状态；没有将开发技术细节加入日常操作流程。

## 实际交互验证

- 使用本地演示账号登录最终单二进制服务。原有生产数据未用于夹具。
- 配额编辑初始显示 125G，未改动时保存禁用。输入无效容量后提交，后台失败时弹窗和输入仍保留，原值提示可见，允许重试。
- 将演示配额改为 126G，实际保存完成后弹窗关闭；重新打开显示 126G。结束前通过界面恢复为 125G。
- 未保存时取消会出现放弃修改提示，不会直接丢失输入。普通取消后焦点回到原行菜单按钮。
- 来源 IP 编辑器带入原有动态单活、1 个来源和 90 秒换绑宽限；关闭后未写入该策略。
- 计费方向选择器、Esc 关闭、用户菜单、导航抽屉、全局搜索、浅色 / 深色 / 跟随系统主题可用。验收后恢复跟随系统并清除临时视口模拟。
- 总览、用户列表与详情、线路、订阅、运维均实际打开。设备、策略和连接页签可切换，空状态正常。390px 用户列表 / 表单与 325–390px 的其他管理页没有页面级横向溢出；宽表格在自身容器中横向滚动。
- 最终浏览器控制台 warn / error 记录为 0。截图宿主偶发的缩放不稳定已通过后台稳定捕获处理，损坏的中间截图未用作通过证据。

## 工程验证与边界

- 修改前后 `go test ./...` 通过；`gofmt -w cmd/sbmgr internal/mesh`、`go vet ./...` 通过。
- 17 项 Vitest / React Testing Library 测试通过，覆盖原值与清空、入口完整性、混合协议、任务确认、失败保留、筛选排序、列显隐、分页、主题、导航、菜单焦点恢复和初始加载日志边界。
- TypeScript、Prettier、Vite 构建，以及关闭 CGO 的 Windows / Linux 内嵌构建通过。
- 本次源码候选隐私检查通过；原有用户测试脚本未修改，不作为本次发布候选。构建、截图、演示数据库均未入库。
- 本轮验收范围是管理界面和本地业务编辑。未执行真实服务器部署、配置应用、代理流量、账号删除或凭据轮换，不能据此声称生产转发已重新验收。

## 实施清单

- [x] 对照选定视觉参考并实现真实业务页面。
- [x] 修复发现的布局、交互和可访问性问题。
- [x] 重截最终版本，复验桌面、窄屏、主题与编辑行为。
- [x] 完成工程验证并保留本地预览。

final result: passed
