# 仓库约束

## 接管顺序

1. 阅读 [README](README.md)、[架构与代码入口](docs/ARCHITECTURE.md)、[开发验证](docs/DEVELOPMENT.md)，再按任务查专题。
2. 检查工作树，保留既有改动；清理仅限本任务生成的文件。
3. 修改前、后均运行 `go test ./...`；Go 代码须经 `gofmt`，提交前运行 `go vet ./...`。

## 必须保持

- **源码与部署资料分离**：Git 只保存源码、测试、文档、CI、构建和部署脚本。数据库及 sidecar、旧 JSON/迁移标记、真实代理配置/YAML、地址与凭据、密钥/证书、导出、日志、备份、产物不得入库；`git add` 前仍须检查，不能只依赖忽略规则。
- **身份与输出**：受管身份只能由程序创建或明确导入，不得接管基础配置凭据。界面、日志、审计、错误、测试快照不得回显密码、UUID、token、私钥或完整代理 JSON。
- **版本职责**：软件版本来自 Git tag，由 `deploy/build-linux.sh` 注入；`sbmgr version` 只读。禁止应用内自更新、下载二进制、维护版本历史或软件回滚；业务状态、基础模板、运行配置备份与失败恢复必须保留。
- **状态写入**：`state.db` 为 SQLite 业务数据库；`state.json` 仅作迁移输入或兼容测试。所有读改写经过跨进程锁、事务、迁移、规范化和 `validateState`。数据库、WAL、锁文件仅管理员可读写，失败不得部分提交。
- **存储结构**：高频统计用结构化表、稳定主键、索引和增量写入；禁止完整 State JSON blob 或每周期清空重写历史。小型策略可用受约束 JSON 列。持久结构变化递增 `stateVersion`；表结构变化另增 schema 版本，提供向前迁移及旧状态/新校验测试。
- **迁移与备份**：JSON 导入保留源文件，锁内二次确认目标不存在，失败清理不完整数据库；SQLite 备份使用一致性快照并通过 `PRAGMA integrity_check`。
- **配置事务**：候选生成 → sing-box 校验 → nftables 校验/快照 → 配置备份 → 原子替换 → 重载/重启；失败回滚。
- **部署事务**：先持久备份状态与配置；旧二进制仅可在外部部署事务内临时保留，成功后删除。业务备份不与历史二进制绑定。

## Web 管理与 CLI

- 日常管理使用内嵌 Web UI；CLI 只保留参数化命令，不恢复 TUI 或交互菜单。
- HTML/CSS/JS 与部署工具编进同一 Go 二进制；运行无需 Node.js 或外部前端资源。
- 中文表单说明作用域、默认值、生效时机、失败行为及是否需应用配置；支持键盘和窄屏。
- Web 管理与只读订阅使用独立 IPC 边界；Linux HTTP 子进程先降权，再接收请求。
- 身份验证、来源校验与写操作 CSRF 校验必须在特权后端执行，不能只依赖前端。
- 新交互至少一项回归测试；认证、越权、凭据隐藏、事务失败及响应边界优先自动测试，页面另做浏览器检查。

## 验证边界

最小验证为 `gofmt -w cmd/sbmgr internal/mesh`、`go vet ./...`、`go test ./...`。涉及 Linux 限速、systemd、部署、证书时，增加 Linux 构建和脚本语法检查。

远端集成默认禁用，须明确授权目标与操作范围。部署前后验证 sbmgr、sing-box、订阅 HTTPS 与真实代理请求；进程状态不代替端到端验收。
