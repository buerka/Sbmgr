# 贡献指南

提交应保持单一目的，并同时包含必要测试和文档。开始前请阅读 `AGENTS.md` 与 `docs/DEVELOPMENT.md`。

拉取请求至少应说明：用户可见变化、状态/配置兼容性、失败与回滚行为、验证命令，以及是否涉及敏感字段。Go 代码需通过 `gofmt`、`go vet ./...` 和 `go test ./...`。

不要在 issue、提交、测试夹具或截图中上传真实服务器地址、UUID、代理账号密码、订阅 token、私钥、证书或用户访问记录。如需复现，请使用明显虚构的保留地址和随机测试凭据。
