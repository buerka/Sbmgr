# Changelog

本项目从 `v0.22.0` 起使用 Git tag 管理软件版本。更早版本由服务器部署记录保留，不在应用内维护二进制历史。

## v0.23.2

- 部署完成前新增 16 秒服务稳定性门禁，连续核对 systemd 活跃状态、主进程 PID、重启次数、二进制版本与 SHA256。
- 稳定窗口后执行有界业务自检；服务崩溃重启、二进制变化、自检失败或信号中断都会在提交发布前恢复旧程序、状态和配置。

## v0.23.1

- 修复 GitHub Actions 默认忽略隐藏 `.dist` 目录、导致已经成功构建的 Linux 发布包无法上传的问题。

## v0.23.0

- 将默认业务存储从单文件 JSON 迁移为嵌入式 SQLite；用户、设备、节点与高频统计使用结构化表、索引和增量 UPSERT。
- 首次启动可在锁内无损导入同目录的旧 `state.json`；原文件与迁移前副本都会保留，失败不切换数据源。
- SQLite 手动/日常备份使用在线备份 API 和业务哈希校验；恢复时先验证完整临时数据库，再整库原子替换并保留损坏文件供排查。
- 安装目录改为由可执行文件或部署脚本位置推导，并支持 `SBMGR_HOME`/`--home`；systemd unit 由模板按实际绝对路径生成。
- 部署流程加入停服锁、影子迁移、数据库校验、unit 路径核对和先验证后回滚；数据库发布使用 no-replace 提交，避免首次迁移竞态。
- root systemd 服务会校验应用目录、sing-box 可执行文件及全部父目录的所有权和写权限，拒绝用户可替换的执行路径。
- 加固远端状态汇总的 SSH 参数、输出边界、终端控制字符清理和快照字段校验。
- 修复 Linux CI 中测试误调用真实 `systemctl`，并升级 GitHub Actions 的 Node 运行时。

## v0.22.0

- 将软件版本管理从 sbmgr 本体移除，取消 CUI/维护命令中的二进制回滚。
- 保留并明确区分用户状态、基础模板、运行配置和限速规则的备份/恢复。
- 版本标识改为由 Git tag/commit 在构建时注入。
- 外部部署仅在部署事务中临时保存旧二进制，成功后清理，失败时自动恢复。
- 增加 CI、开发文档、架构说明、安全策略和 AI 编码代理约束。
