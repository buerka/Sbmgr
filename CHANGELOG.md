# Changelog

记录从 v0.22.0 开始；版本以 Git tag 为准。

## v0.25.0

- 线路支持独立客户端入口及末跳基础出站；主机统一下发从机用户授权、汇总用量，并通过短期租约撤权。
- 订阅按授权线路生成对应入口参数，新增入口配置和手动同步菜单；持久状态升级至 12，SQLite schema 升级至 4。
- 提供独立的三路 Ghana Sticky SOCKS5 封装服务，启动及运行采样验证出口差异，失败或重复时关闭转发。

- 主从接入、可复用的中转/落地线路，逐跳支持 SOCKS5、HY2、WG；分阶段下发、持久事务恢复与结构化 SQLite 迁移。
- WG endpoint 可分配给设备节点，通过共享桥接保留独立计量和限速。
- 订阅交付增加复制/保存地址；首页移除旧节点计数，保留基础身份兼容。
- 拆出拓扑模块，新增跨平台验证入口和协议/恢复回归。

## v0.23.2

- 部署增加 16 秒稳定性门禁，核对 systemd 状态、PID、重启次数、版本及 SHA256，再执行有界业务自检；崩溃、程序变化、自检失败或信号中断时恢复旧程序、状态与配置。

## v0.23.1

- 修复 Actions 忽略隐藏 `.dist` 目录导致 Linux 发布包无法上传。

## v0.23.0

- 默认存储迁移为 SQLite：结构化表、索引与增量 UPSERT；锁内导入旧 JSON，保留源文件和备份，失败不切换。
- 状态备份使用在线 API 与业务哈希；恢复验证临时库后原子替换，保留损坏文件。
- 应用目录支持程序/脚本位置、`SBMGR_HOME`、`--home`；unit 按绝对路径生成。
- 部署增加停服锁、影子迁移、数据库校验、unit 核对、先验证后回滚和 no-replace 数据库提交。
- root 服务校验应用目录、sing-box 及父目录权限；加固 Fleet SSH、输出和快照校验。
- 修复 Linux CI 误调用真实 systemctl，升级 Actions Node 运行时。

## v0.22.0

- 移除应用内软件版本管理和二进制回滚，改由 Git tag/commit 注入构建标识。
- 保留状态、基础模板、运行配置与限速规则备份；外部部署临时保留旧程序，失败恢复、成功清理。
- 增加 CI、开发/架构文档、安全策略与编码约束。
