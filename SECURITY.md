# 安全策略

## 漏洞报告

未修复漏洞通过 GitHub 仓库的 **Security → Report a vulnerability** 私密入口报告。报告应包含受影响版本、影响范围、最小复现和可行的缓解措施。

公开 issue 不适合承载可用的服务器凭据、订阅链接、UUID、私钥、证书、访问记录或未修复漏洞细节。

## 部署资料

运行时数据库、基础配置、生成配置、导出、备份、证书和日志位于部署环境，不属于源码仓库。仓库通过 `.gitignore` 和 CI 隐私检查降低误提交风险；提交者仍需审阅 Git 暂存区。

若敏感值进入 Git 历史，对应凭据应立即轮换。后续根据仓库可见性和影响范围决定是否重写历史；只删除当前版本的文件不能撤销既有暴露。

## 发布边界

安全更新通过 Git commit/tag 和外部部署流程发布。部署时应核对来源、commit、tag 和构建产物 SHA256。sbmgr 不下载、替换或回滚自身二进制。
