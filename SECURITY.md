# 安全策略

请不要在公开 issue 中披露可用的服务器凭据、订阅链接、UUID、私钥、证书或可复现的未修复漏洞细节。优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口，并说明受影响版本、影响范围、最小复现和建议缓解方式。

运行时敏感数据不属于源码仓库。若发现敏感信息曾进入 Git 历史，应立即轮换对应凭据，并在评估影响后净化历史；仅删除当前文件不能撤销泄露。

sbmgr 的安全更新通过 Git tag 发布和外部部署，不通过程序内自更新。部署前应验证来源、Git commit/tag 和构建产物 SHA256。
